# Canonical dependencies are declared in pyproject.toml.
# This file is kept as a convenience for `pip install -r requirements.txt`
# and produces an install equivalent to `pip install -e .[all]`.
#
# Prefer:  pip install -e .[all]   (or `[dev]` for contributors)

# --- core ---
google-genai>=0.3.0
faiss-cpu>=1.7.4
numpy>=1.24
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
rank-bm25>=0.2.2
websockets>=12.0
tenacity>=8.2

# --- reranker (cross-encoder) ---
sentence-transformers>=2.2

# --- repo-map (graph + community detection) ---
tree-sitter>=0.21.0
tree-sitter-python>=0.25.0
tree-sitter-javascript>=0.25.0
tree-sitter-typescript>=0.23.0
python-igraph>=0.11.0
